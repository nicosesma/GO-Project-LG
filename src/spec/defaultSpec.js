"use strict";

describe("An example spec", function() {
  it("confirms 1 === 1", function() {
    expect(1).toEqual(1);
  })

  it("confirms 1 != 2", function() {
    expect(1).not.toEqual(2);
  });
})
